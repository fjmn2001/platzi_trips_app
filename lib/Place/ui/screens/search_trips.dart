import 'package:flutter/material.dart';

class SearhTrips extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      child: Placeholder(),
    );
  }

}